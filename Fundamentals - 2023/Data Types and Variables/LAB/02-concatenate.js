function concatenate(first, last, delimiter) {
    let result = first + delimiter + last;
    console.log(result);
}

concatenate('John', 'Smith', '->');
concatenate('Jan', 'White', '<->');
concatenate('Linda', 'Terry', '=>');